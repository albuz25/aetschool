To replace the site favicon, overwrite this file:

`public/favicon/favicon.png`

Use a square PNG (512 × 512 is ideal). After replacing it, also copy it to:

- `app/icon.png`
- `app/apple-icon.png`

Browsers use `app/favicon.ico` and `app/icon.png` as the live tab icon.
